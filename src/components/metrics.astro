---
const metrics = [
  { value: "€2.4B", label: "Properties Under Management" },
  { value: "94%", label: "Client Satisfaction Rate" },
  { value: "127", label: "International Clients Served" },
  { value: "4.6%", label: "Average Rental Yield" },
];
---

<section class="bg-popover border-gold-dim border-y">
  <div class="container grid gap-10 py-24! md:grid-cols-2 lg:grid-cols-4">
    {
      metrics.map((metric) => (
        <div class="reveal space-y-2 text-center">
          <p class="heading-2 text-gold">{metric.value}</p>
          <p class="tagline text-neutral-400!">{metric.label}</p>
        </div>
      ))
    }
  </div>
</section>

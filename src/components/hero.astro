---
import { CalButton } from "./cal-button";
import { Button } from "./ui/button";
---

<section
  id="hero"
  class="flex min-h-screen items-center justify-center overflow-hidden px-5 py-32 md:px-8"
>
  <div class="animate-fade-in-up max-w-6xl text-center text-balance">
    <p
      class="text-gold border-gold-dim mx-auto w-fit border px-4 py-2 font-mono text-xs tracking-widest uppercase md:text-sm"
    >
      Investment Property Consulting
    </p>

    <h1
      class="mt-6 font-serif text-5xl leading-tight tracking-tight md:text-7xl"
    >
      Germany's real estate market speaks German.
      <em class="text-gold italic">Your wealth shouldn't have to.</em>
    </h1>

    <p class="mx-auto mt-6 text-lg text-neutral-400">
      From strategy to purchase to hands-off management. We guide international
      professionals through German residential real estate with the same
      precision the locals expect.
    </p>

    <div class="mt-8 flex flex-wrap justify-center gap-4">
      <CalButton className="max-md:w-full" client:load>
        Schedule Free Consultation
      </CalButton>
      <a href="/suitability-check">
        <Button variant="outline" className="max-md:w-full">
          Discover Your Options
        </Button>
      </a>
    </div>

    <div class="mt-12 space-y-6">
      <p class="font-mono text-xs tracking-wide text-neutral-400 uppercase">
        Trusted by professionals from
      </p>
      <div class="flex flex-wrap justify-center gap-6 md:gap-12">
        {
          ["Silicon Valley", "London", "Dubai", "Singapore", "Zurich"].map(
            (location) => (
              <p class="flex items-center gap-2 font-serif text-sm text-neutral-500">
                <span class="text-gold text-[0.6rem]">◆</span>
                {location}
              </p>
            ),
          )
        }
      </div>
    </div>
  </div>
</section>

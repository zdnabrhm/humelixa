---
import { Button } from "./ui/button";
---

<section id="consultation" class="bg-background">
  <div class="relative container overflow-hidden text-center">
    <!-- Background Glow -->
    <div
      class="pointer-events-none absolute top-1/2 left-1/2 h-150 w-150 -translate-x-1/2 -translate-y-1/2 bg-[radial-gradient(ellipse,oklch(0.76_0.12_85/15%)_0%,transparent_70%)]"
    >
    </div>

    <div class="reveal relative z-10 mx-auto max-w-2xl space-y-4">
      <p class="tagline">Start Your Journey</p>
      <h2 class="heading-2">
        Ready to build German wealth <em class="text-gold italic"
          >with expert guidance?</em
        >
      </h2>
      <p class="text-lg text-balance text-neutral-400">
        Book a free 30-minute consultation. No obligation, no hard sell—just
        honest assessment of your options.
      </p>
    </div>

    <div class="mt-8 flex flex-wrap justify-center gap-4">
      <Button className="max-md:w-full">Schedule Free Consultation</Button>
      <Button variant="outline" className="max-md:w-full"
        >Discover Your Options</Button
      >
    </div>
  </div>
</section>
